

import 'package:flutter/material.dart';


class CustomLinearContainer extends StatelessWidget {
  const CustomLinearContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 1,
      color: Colors.grey,
    );
  }
}
